# 图片批量上传 → 拉伸为 A4 → 生成 PDF 打印工具

## 1. 项目目标

提供一个工具，支持用户批量上传图片，将每张图片自动拉伸（缩放）到 A4 纸张大小，并生成一份可打印的 PDF 文件。

## 2. 需求描述

| 编号 | 需求项 | 详细说明 | 优先级 |
|------|--------|----------|--------|
| R1 | 批量上传 | 支持一次选择多张图片（常见格式：JPG / PNG / BMP / WebP 等） | 高 |
| R2 | 图片预览 | 上传后可预览图片列表，可查看、调整顺序、删除单张 | 高 |
| R3 | 拉伸到 A4 | 将每张图片等比或拉伸缩放至 A4 尺寸（210mm × 297mm，约 2480 × 3508 px @300dpi） | 高 |
| R4 | 布局方式 | 每张图片占一页 A4，默认铺满整页 | 高 |
| R5 | 生成 PDF | 将所有图片按顺序合并导出为一个 PDF 文件 | 高 |
| R6 | 打印支持 | 生成的 PDF 为 A4 尺寸，可直接打印，无需二次调整 | 高 |
| R7 | 图片方向处理 | 自动识别横竖版，旋转后适配 A4 页面 | 中 |
| R8 | 排序与调整 | 支持拖拽或上移/下移调整图片顺序 | 中 |
| R9 | 进度与提示 | 上传、处理、导出过程中有进度提示和错误提示 | 中 |

## 3. 关键规则

- **A4 尺寸**：210 mm × 297 mm
- **像素换算**（按 300 DPI）：
  - 宽度 ≈ 2480 px
  - 高度 ≈ 3508 px
- **拉伸模式**：默认将图片拉伸铺满整页 A4（可失真拉伸），后续可扩展为「等比缩放留白」模式。
- **页边距**：默认无边距铺满；可预留配置项。

## 4. 功能流程

```
选择/批量上传图片
      ↓
预览列表（可排序、删除）
      ↓
逐张拉伸至 A4 尺寸
      ↓
生成 PDF（每图一页）
      ↓
下载 / 打印
```

## 5. 界面设计风格

采用**国际主义风格**（International Typographic Style / Swiss Style），设计要点：

| 编号 | 设计要素 | 说明 |
|------|----------|------|
| D1 | 网格系统 | 基于严格的网格布局，对齐清晰、结构理性 |
| D2 | 无衬线字体 | 使用无衬线字体（如 Helvetica / Arial / Inter 等），字重对比鲜明 |
| D3 | 扁平化 | 无渐变、无阴影、无装饰，纯扁平色块 |
| D4 | 左对齐排版 | 文字左对齐、避免居中，信息层级通过字号/字重区分 |
| D5 | 色彩 | 少而克制的用色，主色 1 个 + 黑白灰，可用高饱和色做强调（如红/蓝） |
| D6 | 留白 | 大量留白，突出内容本身 |
| D7 | 摄影/大图 | 以图片本身为视觉主体，界面尽量弱化 |

## 6. 非功能需求

| 编号 | 需求项 | 说明 |
|------|--------|------|
| N1 | 易用性 | 界面简洁，用户无需专业知识即可操作 |
| N2 | 性能 | 支持一次处理几十张图片，处理时间合理 |
| N3 | 兼容性 | 支持主流浏览器 / 操作系统 |
| N4 | 隐私 | 图片在本地处理（或说明是否上传服务器），不泄露用户数据 |

## 7. 待确认问题

1. 拉伸方式：是「强制拉伸铺满（可能变形）」还是「等比缩放留白」？
2. 是否需要自定义页边距、图片居中方式？
3. 工具形态：网页版、桌面应用，还是命令行脚本？
4. 是否需要在 PDF 中加页码、文件名等标注？
5. 图片是本地处理还是需要上传到服务器处理？

## 8. 验收标准

- [ ] 可一次性上传多张图片
- [ ] 每张图片生成一页 A4 尺寸的 PDF 页面
- [ ] 输出 PDF 页面尺寸为 210mm × 297mm
- [ ] PDF 可直接打印，图片铺满 A4 页面
- [ ] 支持调整图片顺序与删除
- [ ] 界面符合国际主义风格（网格、无衬线、扁平、左对齐、克制用色）

## 6. 待确认问题

1. 拉伸方式：是「强制拉伸铺满（可能变形）」还是「等比缩放留白」？
2. 是否需要自定义页边距、图片居中方式？
3. 工具形态：网页版、桌面应用，还是命令行脚本？
4. 是否需要在 PDF 中加页码、文件名等标注？
5. 图片是本地处理还是需要上传到服务器处理？

## 7. 验收标准

- [ ] 可一次性上传多张图片
- [ ] 每张图片生成一页 A4 尺寸的 PDF 页面
- [ ] 输出 PDF 页面尺寸为 210mm × 297mm
- [ ] PDF 可直接打印，图片铺满 A4 页面
- [ ] 支持调整图片顺序与删除
